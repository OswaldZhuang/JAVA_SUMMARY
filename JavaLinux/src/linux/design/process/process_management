对于Linux而言，线程即是特殊的进程，其特殊之处在于某些资源的共享

进程上下文：当程序执行了系统调用或者发生异常而陷入内核空间（即内核代表程序执行某些操作），那么内核就处在进程上下文中

内核将进程的列表存放在"任务队列"（task list）中，其数据结构为双向循环链表，链表的每一项为"进程描述符"

进程的创建通过fork()函数来实现（实际上是父进程创建子进程），而fork()依赖于clone()系统调用

进程的执行（包括装载执行映象到地址空间和执行代码）依赖于exec()系统调用

进程终结依靠exit()系统调用来完成