package vm.memory;

public class AboutHeap {
	
	/*
	 * 堆是被所有线程所共享的内存区域
	 * 其作用是存放对象
	 */
    
    /*
     * 堆的结构:
     * 1.新生代:
     *      1.1 eden区:
     *      1.2 servivor区:
     *          1.2.1 s0(from)
     *          1.2.2 s1(to)
     * 2.老年代(tenured):
     */


}
