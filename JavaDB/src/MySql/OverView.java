package MySql;

public class OverView {
    /**
     * MySql是单进程多线程的
     */

    /**
     * mysql 目录:
     * /usr/local/mysql
     * 配置文件:
     * /usr/local/mysql/support-files/my.cnf (在mysql v5未找到)
     * /etc/mysql/my.cnf (默认不存在，需要自己创建)
     */

    /**
     * mac下启动mysql
     * sudo .le & (在/usr/local/mysql/bin下)
     *
     * mac下链接mysql
     * /usr/local/mysql/bin/mysql -u <user_name> -p <password>
     */
}
